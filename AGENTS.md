# 开发约定

## 范围

本仓库是 Go 模块 `github.com/c-poet/go-app`。通用运行时能力放在 `core` 包中，保持其 API 小而稳定。

## 修改要求

- 提交前对修改过的 Go 文件执行 `gofmt`。
- 新增或变更依赖后执行 `go mod tidy`，并一并提交 `go.mod` 与 `go.sum`。
- 修改后运行 `go test ./...`。
- 新增或更新功能逻辑时，必须同步更新相关说明文档；涉及公开 API、使用方式或项目结构时，至少更新 `README.md`。
- 不要提交构建产物、日志、运行时数据或本地 IDE 配置。

## 设计约束

- `core.InitApp` 使用进程级单例；不要在并行测试或同一进程中假设它可以用不同选项重复初始化。
- 配置键支持点号分隔的嵌套路径；新增配置相关 API 时保持这一行为一致。
- 运行时文件应通过 `GetWdHome` 或相关工作目录帮助函数定位，避免硬编码系统路径。
